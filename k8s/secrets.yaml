# This file is only a template showing which keys the application requires.
# The deploy job in .github/workflows/ci.yml creates the real Kubernetes Secret from GitHub Secrets.
apiVersion: v1
kind: Secret
metadata:
  name: journal-api-secrets
  namespace: journal-api
type: Opaque
stringData:
  DATABASE_URL: REPLACE_ME
  OPENAI_API_KEY: REPLACE_ME
